/title() Uživatelská dokumentace /x

/subtitle() Spouštění /x

/section
Program se spouští příkazem: /bold() dotnet run [vstupní soubor] [setup soubor] /x

/section
Kde vstupní soubor je povinou součástí, ale setup soubor je implicitním parametrem, pokud se nezadá, tak je nahrazen globálním Setup.json.

/section
Pro správné fungování programu je potřeba ještě nad rámec klasické distribuce dotnet doinstalovat balíček /bold() iText /x verze 9.1.0 nebo vyšší.
Potřebný balíček můžete stáhnout pomocí následujícího příkazu:

/list()
/listitem()
sem přidat příkaz 
/x
/x

/subtitle() Ovládání /x

/section 
Program nemá žádná dodatečné interaktivní ovládání. Uživatel může chod programu upravovat při volání programu pomocí dvou vstupních parametrů. 
Jediná další možnost personalizace programu se sktrývá právě v druhém parametru, poněvadž si může uživatel vytvořit své vlastní textové CzeTex funkce, která následně může v textu využívat. 

/section()
/bold() Doporučení: Setup soubory musí mít přesnou strukturu a správné odkazy na interní funkce, proto se doporučuje, aby se při vytváření personalizovaných setup souborů využila kopie globálního a pouze upravili klíčová slova dle vlastní preferencí. /x